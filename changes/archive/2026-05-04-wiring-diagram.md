# Wiring diagram

**Mode:** Wander

## Intent

Add an ASCII pin-wiring diagram to the README for two Raspberry Pi Pico 2 boards (RP2350, non-wireless variant), showing the minimum connections needed for the HalfWire architecture: the one-way UART link, common ground, power topology, and where each end's USB connects.

The diagram should make the diode property visible at a glance — the missing return wire is the load-bearing detail and should be drawn explicitly rather than implied.

## Conclusion

Wiring section added to README between **What it is** and **Why**. Next change spun out: `derisk-pico-a-usb-host`.

